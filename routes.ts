export const publicPaths = ["/"];

export const authPaths = ["/auth/signin", "/auth/signup"];

// always allow never block
export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";
